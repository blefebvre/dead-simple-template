dead-simple-template
====================

No frills template for testing CQ5 components in isolation

Install
========

To install this package to a CQ instance running on :4502:
	mvn clean install vault:install